# Optional manual patches

Daily CI applies **bivlked old→new** unified diffs onto Freedom’s English AWG helpers (`tools/sync_upstream_pr.sh`).

Add a `*.patch` here only when the automated diff fails and you need a maintained overlay on top of a specific bivlked tag.
